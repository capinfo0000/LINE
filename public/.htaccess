# public/ が直接ドキュメントルートになる運用向けの最小防御。
# ドットファイル（.user.ini / .env 等）を配信しない。
<FilesMatch "^\.">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
